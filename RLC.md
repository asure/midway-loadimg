# RLC Run-Length Code — Narc DMA1 Legacy Encoding

This document describes the **RLC** (Run-Length Code) format used by the
original LOAD.EXE for 4-color images in Williams/Midway DMA1-era arcade
games (Narc, 1988). It is NOT related to PackBits, LZ4, or other modern
compression schemes.

---

## Quick Start: Testing RLC Encoding

### Prerequisites
- DOSBox installed
- `loadimg` built from this repo
- Files in `worknarc/` (NARCMSU.IMG, NARCMUGS.IMG, narcfbi.IMG, NARCATT.IMG, NARCMUGS.LOD)

### Step 1: Generate Reference with LOAD.EXE (DOSBox)

The reference IRW and TBL were generated by the original LOAD.EXE:
```bash
cd loadimg
dosbox
# Inside DOSBox:
# mount c worknarc
# LOADW NARCMUGS /P /F=C:\TMP /T=C:\TMP /V5 >C:\TMP\NARCMUGS_OUT.TXT
```

Reference files are in `refnarc/` (TBL) and `worknarc/NARCMUG.BIN` (IRW data).

### Step 2: Run Our Tool
```bash
../build/loadimg NARCMUGS.LOD /OLD /T
# Produces NARCMUGS.TBL, NARCMUGS.IRW, IMGPAL.ASM
```

### Step 3: Compare
```bash
diff ../refnarc/NARCMUGS.TBL NARCMUGS.TBL
```

---

## Format Specification

### Byte-level Encoding

Each RLC byte is one of two types:

**Run byte** (MSB = 1):
```
[1][rrrrr][cc]
  rrrrr = 5-bit run length code
  cc    = 2-bit color value (0-3)

  r = 0      → 68 pixels
  r = 1..31  → r + 3 pixels  (4..34 pixels)
```

**Literal byte** (MSB = 0):
```
[00][c2][c1][c0]
  c0 = pixel 0 (bits 1-0)
  c1 = pixel 1 (bits 3-2)
  c2 = pixel 2 (bits 5-4)
```

Literal bytes always encode exactly 3 pixels, even at the end of the image
(the decoder reads 3 pixels and subtracts 3 from the remaining count).

### Decoder Logic (from `UnpackRLC` in NARCMUGS.ASM, line 2246)

```
A0 = ptr to image frame (contains ISIZEX, ISIZEY, ISAG, palette)
A2 = destination address (bit-addressable, 2bpp)

1. pixel_count = ISIZEX × ISIZEY
2. src = ISAG - 8  (pre-decrement for loop)
3. Loop:
     src += 8 (= advance 1 byte)
     byte = read_byte(src)
     if byte >= 0:  // MSB = 0 → literal
         store byte at dest
         dest += 6 bits
         pixel_count -= 3
     else:          // MSB = 1 → run
         r = (byte >> 2) & 0x1f
         run = (r == 0) ? 68 : r + 3
         color = byte & 3
         // Clamp to remaining pixels
         if run > pixel_count: run = pixel_count
         // Long run optimization (≥16): write word-expanded color
         dest = write_n_pixels(color, run, dest)
         pixel_count -= run
4. Stop when pixel_count <= 0
```

### Encoder Logic (from `rlc_encode` in loadimg.c)

```
Input: 8-bit pixels from IMG, stride-width buffer (IMG_STRIDE(w) × h)
Output: RLC byte stream to IRW

1. total_pixels = w × h
2. Read w pixels per row from stride-width buffer
3. Reduce to 2bpp via `pixel & 3`
4. Flat RLE (runs can span row boundaries):
   while pos < total_pixels:
       cur = pixels[pos]
       run = count_consecutive(pixels, pos, cur)
       if run >= 4:
           while run > 0:
               emit = min(run, 68)  // max single-byte run
               if emit > 34: emit = 34  // split large runs
               r = (emit == 68) ? 0 : emit - 3
               write_byte(0x80 | (r << 2) | cur)
               pos += emit
               run -= emit
       else:
           c0, c1, c2 = pixels[pos:pos+3]
           write_byte((c2 << 4) | (c1 << 2) | c0)
           pos += 3
```

---

## RLC in the LOD File

The `RLC>` directive toggles RLC encoding on/off:

```
PPP> 6
---> warntxt           ; 6bpp packed (NOT RLC)
PPP> 8
RLC>
---> filestxt,authtxt  ; RLC encoded (2bpp)
NARCMUGS.IMG
---> gangmug,...        ; RLC encoded (PPP=8 still active, RLC still on)
RLC>
```

When RLC is active (`g.rlc = 1`), `encode_image()` calls `rlc_encode()`
instead of the normal ZOF/ZON pixel encoding.

---

## The Quantization Puzzle

The exact algorithm LOAD.EXE uses to convert 8-bit IMG pixel values to
2-bit RLC colors is **unknown**. What we know:

### What it is NOT
- NOT `pixel & 3` (low 2 bits) — only 53/900 matching bytes
- NOT top 2 bits — 0/900
- NOT RGB Euclidean distance to palette colors — 53/900
- NOT median-cut or popularity quantization — 53/900
- NOT Floyd-Steinberg dithering — tested various patterns
- NOT simple transparent fill (up/down/left/right) — 53-188/900
- NOT any bit position combination — tested all 28 pairs

### What works BEST so far

**Best size match**: `stride-180 + map {0,1}→1, {2}→2` — produces EXACTLY
900 bytes (matching reference), 516/900 matching bytes. Problem: breaks
multi-color images.

**Best cascade**: `tight + pixel & 3` — 959 bytes vs 900, but produces
only +25B cascade in authtxt (vs +1B with stride mapping).

### Key clues from analysis

1. **Pixel 177 is stride padding** (value 0, transparent). Reference encodes
   it as color 1, our encoder (pixel & 3) gives 0. This causes the first
   byte-level divergence at byte 3.

2. **All 384 mismatch positions are at stride boundaries** (every 180 pixels).
   The reference handles the 3-byte stride padding at each row end differently.

3. **The decoded RLC pixel count** for some images exceeds w×h (e.g., filestxt
   decodes to 7106 vs expected 7080). The decoder stops at ISIZEX×ISIZEY
   pixels — trailing bytes are ignored.

4. **Same palette index maps to DIFFERENT RLC colors** at different positions
   in the decoded reference output. This means the mapping is NOT based on
   the palette index value alone.

### Test Vectors for the AI

**Input data** for filestxt (177×40, stride=180, 8bpp):
- IMG pixels: `worknarc/NARCMSU.IMG` at offset `0x2632a` (old-format record)
- Dimensions: w=177, h=40, stride=180
- Palette: `fbitxt4` (5 colors: black, dark green, bright green, brown, white)
- IMG pixel values used: 0 (black/transparent), 1 (dark green), 2 (bright green)

**Expected RLC output**: `worknarc/NARCMUG.BIN` at bytes 1950-2850 (900 bytes)

**Expected decoded pixels**: Should produce exactly 177×40 = 7080 pixels
when decoded by `UnpackRLC` (the decoder stops at ISIZEX×ISIZEY).

**First byte where tools diverge**: byte 3
- Reference: `0x99` = run 9× color 1 (pixels 170-178)
- Our tool: `0xa5` = run 12× color 1 (pixels 170-181)

The 3-pixel difference comes from stride padding positions 177-179
(value 0 in IMG). The reference maps them differently.

### How to Decode RLC Data (Python)

```python
def decode_rlc(data, max_pixels=None):
    """Decode RLC byte stream to 2-bit pixels."""
    pixels = []
    for b in data:
        if b & 0x80:  # Run
            r = (b >> 2) & 0x1f
            run = 68 if r == 0 else r + 3
            color = b & 3
            if max_pixels:
                run = min(run, max_pixels - len(pixels))
            pixels.extend([color] * run)
        else:  # Literal
            c0 = b & 3
            c1 = (b >> 2) & 3
            c2 = (b >> 4) & 3
            remaining = 3
            if max_pixels:
                remaining = min(3, max_pixels - len(pixels))
            if remaining >= 1: pixels.append(c0)
            if remaining >= 2: pixels.append(c1)
            if remaining >= 3: pixels.append(c2)
        if max_pixels and len(pixels) >= max_pixels:
            break
    return pixels

# Decode filestxt reference
with open('worknarc/NARCMUG.BIN', 'rb') as f:
    data = f.read()
rlc_data = data[1950:2850]  # filestxt RLC data
pixels = decode_rlc(rlc_data, max_pixels=177*40)
print(f'Decoded {len(pixels)} pixels')
```

### How to Encode RLC Data (Python)

```python
def encode_rlc(pixels, w, h):
    """Encode 2-bit pixels to RLC byte stream."""
    total = len(pixels)  # should be w * h
    out = bytearray()
    pos = 0
    while pos < total:
        cur = pixels[pos]
        run = 1
        while pos + run < total and pixels[pos + run] == cur:
            run += 1
        if run >= 4:
            while run > 0:
                this = 68 if run >= 68 else (34 if run > 34 else run)
                r = 0 if this >= 68 else this - 3
                out.append(0x80 | ((r & 0x1f) << 2) | (cur & 3))
                pos += this
                run -= this
        else:
            c0 = pixels[pos]
            c1 = pixels[pos+1] if pos+1 < total else 0
            c2 = pixels[pos+2] if pos+2 < total else 0
            out.append((c2 << 4) | (c1 << 2) | c0)
            pos += 3
    return bytes(out)
```

---

## Comparison: Our Encoding vs Reference

Our encoder (tight + pixel & 3):
- filestxt: 959 bytes (reference: 900)
- authtxt: +25B cascade
- gangmug: +61B cascade
- All subsequent mugshots: cascade grows

The pixel quantization function in `rlc_encode` reads:
```c
p2[y * w + x] = row[x] & 3;
```

This is the line to experiment with. Replace `row[x] & 3` with different
mappings to test:
```c
// Current:
p2[y * w + x] = row[x] & 3;

// Map 0/1→1, 2→2 (best size match for filestxt):
int v = row[x] & 3;
p2[y * w + x] = (v == 2) ? 2 : 1;

// Using actual palette colors:
int vi = row[x];
uint16_t pal_entry = palette[vi];  // RGB565
// ... compute distance to 4 target colors
```

The stride at the pixel read point is `IMG_STRIDE(rec->w)` = `(w + 3) & ~3`.
For filestxt: stride = `(177 + 3) & ~3` = 180.

---

## Files Reference

| File | Path | Description |
|------|------|-------------|
| RLC encoder | `src/loadimg.c:1100-1141` | `rlc_encode()` function |
| RLC call site | `src/loadimg.c:1189-1193` | Where `rlc_encode()` is invoked |
| RLC> handler | `src/loadimg.c:2143` | `else if (!strncmp(upper, "RLC>", 4))` |
| UnpackRLC (ASM) | `binary/NARCMUGS.ASM:2246-2342` | Original decompressor source |
| NARCMUGS.LOD | `worknarc/NARCMUGS.LOD` | Test LOD file |
| NARCMUGS.BIN | `worknarc/NARCMUG.BIN` | Reference IRW data section |
| NARCMUGS ref TBL | `refnarc/NARCMUGS.TBL` | Reference TBL output |
| Narc MSU IMG | `worknarc/NARCMSU.IMG` | Source IMG for filestxt/authtxt |
